# 設計メモ — Gmail ラベル自動振り分け

作成日: 2026-08-19 / 対象アカウント: hurwa8@gmail.com

---

## 1. 現状の棚卸し

Gmail API で実データを取得した結果 (2026-08-19 時点)。

| 指標 | 値 |
| --- | --- |
| 受信トレイ | **11,642 通** (未読 9,225 通) |
| 全体の未読 | 10,489 通 |
| ユーザー定義ラベル | 39 個 |
| 大項目 (トップレベル) | 5 個 — `Australia` / `ファイナンス` / `プロモーション` / `Schedules` / `Authentication` |
| 3 階層まで使っているラベル | **1 個だけ** (`Australia/AWX/Payslip`) |

受信トレイに 11,642 通が残っているのが、フィルタが機能していないことの直接的な証拠です。

### 1.1 見つかった問題

**① 軸が混ざっている (最大の構造問題)**

大項目の 5 つが、それぞれ**別の分類軸**になっています。

| 大項目 | 実際の軸 |
| --- | --- |
| `プロモーション` | メールの**種別** (販促かどうか) |
| `Australia` | **拠点 / 生活圏** |
| `ファイナンス` | **ドメイン** (お金) |
| `Schedules` / `Authentication` | メールの**種別** |

軸が違うものを同じ階層に並べたので、同じ概念が両方に現れています。

- `プロモーション/Stores` (480) ↔ `Australia/Stores` (2,039)
- `プロモーション/Meal` (1,497) ↔ `Australia/Meal` (0)
- `プロモーション/Employment` (5,418) ↔ `Australia/Employment` (1,223)

「オーストラリアの店からの販促メール」がどちらに入るべきか決まらない。これが
**振り分けルールを書くたびに迷い、ルールが増殖した根本原因**です。

**② `from:` のワイルドカードが効いていない**

スクリーンショットのフィルタに以下の記述があります。

```
from:({ from: *.agoda-emails.com  from: *@jalan\.net  ... })
```

Gmail のフィルタは **glob (`*`) をサポートしていません**。`*` はワイルドカードとして
解釈されず、部分一致的に処理されるため、意図しないマッチ / ノーマッチが起きます。
これが「うまく動かない時がある」の有力な原因です。

**③ フィルタの上限に近づく書き方をしている**

Gmail は 1 アカウント **1,000 フィルタ**、1 フィルタあたり**クエリ 1,500 文字**が上限です。
現在の「1 つのフィルタに `from:` を `{ }` で大量に束ねる」書き方は、フィルタ数の上限を
避けるための苦肉の策ですが、束が育つと 1,500 文字上限にぶつかって静かに壊れます。

**④ 遡及しない・ログが残らない・失敗に気づけない**

- フィルタは**受信時にしか走らない**。ルールを直しても過去メールは直らない。
- 何がなぜ振り分けられたかの**記録がない**。デバッグができない。
- 「受信トレイをスキップ + 既読にする」が効くとメールが**静かに消える**ので、
  誤爆しても気づけない。

**⑤ 死んだラベルと命名ゆれ**

- 件数 0 のラベル: `Australia/Accommodations`, `Australia/Meal`, および親のみのプレースホルダ 5 個
- `Authentication/Login` は **1 通**しかない (認証メールが分類できていない)
- `プロモーション/Creater` は `Creator` の綴り間違い
- `Cashback rewards` だけ空白入り・小文字始まり
- 日英混在: `楽天カード` / `楽天証券` vs `CreditCards` / `DebitCards`

---

## 2. 決定した方針

| 論点 | 決定 |
| --- | --- |
| ラベルの軸 | **A: 種別を大項目・拠点は並行ラベル** |
| 既存 11,642 通 | **直近 1 年に遡及適用** (ドライラン先行) |
| 週次 AI | **Claude の週次 Routine** |
| 既存 Gmail フィルタ | **凍結して並走 → 段階移行** |

### 2.1 なぜ「拠点を並行ラベル」なのか

**Gmail はフォルダではなくタグ**です。1 通に複数ラベルを付けられます。
無理に 1 本の木に押し込むから破綻するので、軸を 2 本に分けます。

```
軸1: 種別 (階層 3 段)          軸2: 拠点 (フラット)
  Finance/Cards/Rakuten         @AU   ← 凍結済み
  Promotions/Stores
  Work/AWX/Payslips
```

「オーストラリアの店からの販促メール」は `Promotions/Stores` **と** `@AU` の
両方が付きます。どちらに入れるか迷う問題が構造的に消えます。

**ワーホリは終了済み**なので、`@AU` はアクティブな分類軸ではなく
最初から「過去を掘り出すためのマーカー」です。移行時に既存メールへ遡及付与するだけで、
新規メールには付けません。AU 関連ルールは `凍結日` を移行日に設定して取り込みます。
ラベル自体は過去メールの検索性のために残します。

`凍結日` と `有効 = FALSE` は別物です。`有効 = FALSE` はルールを完全に止めますが、
`凍結日` は**過去メールへの遡及適用は許し、新規メールにだけ適用しない**。
既存の 4,944 通に `@AU` を付けたうえで、今後は増やさない、という挙動がこれで作れます。

**`@JP` は作りません。** `@AU` でないものは全部 JP なので、9,000 通以上に
無意味なタグが付くだけです。3 つ目の拠点が出てきたら、そのとき追加します。

### 2.2 新しいラベル体系

大項目 = メールの種別、中項目 = ジャンル、小項目 = ブランド。拠点は階層に混ぜず並行ラベルにする。

**大項目 14 個の一覧と意味、中項目の切り方、命名規約は `docs/naming.md` にあります。**
ここには決定の理由だけを残します。

- **ラベル名は英語**にした。大項目・中項目を日本語と英語で混ぜると並び順が読めなくなるため。
  小項目のブランド名だけ原語 (`Rakuten` / `Amazon` / `AWX`)
- **除外して既読にしてよいのは `Promotions` だけ**にした。
  これを緩めると、Gmail フィルタで起きていた「静かに消える」が再発する

### 2.3 求人を `Promotions/Jobs` と `Work` に二分する

求人系は 6,641 通あり、全ラベル中で最大の塊です。ここを
「応募したやつ / リクルート系 / 関心が高いやつ / バイト」で並列に分けると、
**また軸が混ざります** (自分の行動 / 送信元 / 主観評価 / 雇用形態)。

一番効く境界線は**「自分がアクションしたかどうか」**です。応募済みは埋もれたら困る、
求人アラートは読み流すもの。扱いが正反対なので大項目のレベルで割ります。

```
Promotions/Jobs/Agencies  リクルート系      ┐ 受信トレイ除外 + 既読
Promotions/Jobs/Temp      派遣・バイト      │ (読み流す側)
Promotions/Jobs/Alerts    一般の求人アラート ┘

Work/Applications         応募済み          ┐ 受信トレイに残す
Work/Shortlist            関心が高い         │ (自分が動いている側)
Work/AWX                  勤務先            ┘
```

**`Work/Applications` は自動判定できます。** 最強のシグナルは
「スレッドに自分の送信メールが含まれているか」。`GmailThread.getMessages()` を回して
`from` が自分なら、そのスレッドは自分がアクションを起こしたものだと判定できます。
求人アラートは一方通行なので、この 1 点でほぼ切り分けられます。
補助として件名パターン (`ご応募` / `選考` / `Application received` / `Thanks for applying`) も併用。

**`Work/Shortlist` はスターを入力装置にします。** 「関心が高い」は主観なので機械判定できません。
毎回ラベルを手で付けるのも面倒です。そこで:

```
Promotions/Jobs/* のメールにスターを付ける
        ↓ 次の GAS 実行 (15分以内)
Work/Shortlist に昇格 + 受信トレイに戻す + スターは外す
        ↓ 週次
Claude が Watching の傾向を学習し、似た求人を次から拾うルールを提案
```

スマホでもワンタップで済み、状態はラベルとして残り、続けるほど手作業が減ります。
承認ループと同じ思想です。

ラベル体系の定義は `docs/naming.md` にあります。

---

## 3. アーキテクチャ

```
┌─────────────────────────────────────────────────────────┐
│  Google スプレッドシート  "Gmail Label DB"   ← 真実の源   │
│  labels / rules / senders / log / log_archive             │
│  unmatched / proposals / config                          │
└───────────┬─────────────────────────────▲───────────────┘
            │ 読む                          │ 書く
            ▼                              │
┌─────────────────────────────┐            │
│  Google Apps Script          │            │
│  ・15分ごと: 新着に適用       │            │
│  ・週次:    集計 → unmatched │────────────┘
│  ・月次:    遡及の取りこぼし回収 │
└───────────┬─────────────────┘
            │ GmailApp
            ▼
      ┌───────────┐        ┌──────────────────────────┐
      │  Gmail    │        │  Claude 週次 Routine      │
      └───────────┘        │  unmatched を読む         │
                           │  → proposals に提案を書く  │
                           └──────────────────────────┘
                                        │
                                   人間が承認列を
                                   プルダウンで変更
                                        │
                                        ▼
                             次回 GAS 実行が rules に反映
```

**承認ループがこの設計の要**です。「登録が面倒」という課題に対して、
ユーザーの操作を**プルダウンを 1 つ変えるだけ**に圧縮します。

---

## 4. スプレッドシートのシート構成

**列の定義は `src/schema.ts` だけに置きます。** ここに再掲すると必ず食い違うので、
役割と、なぜそのシートが要るのかだけを書きます。

| シート | 役割 |
| --- | --- |
| `labels` | ラベルマスタ。Gmail の実態を `importCurrentState()` が写す。`rules` のラベル列の入力規則の元になる |
| `rules` | メーリングリスト ↔ ラベル。**振り分けの真実の源** |
| `senders` | 送信元マスタ。運営元とサービスを分けて持ち、同じ会社の複数サービスをまとめて見る |
| `log` | 実行ログ。件名は先頭 60 文字まで、本文は記録しない |
| `log_archive` | 6 か月より古い `log` 行の退避先 |
| `unmatched` | 受信トレイの未分類を Gmail のカテゴリ別に集計。**週次 AI の入力** |
| `proposals` | AI の提案と承認。**承認列のプルダウンを変えるだけで `rules` に反映される** |
| `config` | 実行時の設定。`DRY_RUN` など。コード側の既定より優先される |

### なぜ `rules.種別 = list_id` を第一級に扱うか

メーリングリストは `List-Id` / `List-Unsubscribe` ヘッダを持つので、Gmail 検索の
`list:` 演算子で確実に引けます。`from:` のワイルドカードは Gmail が解釈しないため、
桁違いに安定します。「メーリングリストと項目を引っ付けておく」への直接の答えがこれです。

`最終マッチ日` があると「半年マッチしていない = 死んだルール」が一目で分かり、
ルールの増殖に歯止めがかかります。

### 受信トレイの保持期間をルール単位で持つ理由

「記録は残したいが毎回開く必要はない」メール (カードや口座の利用通知) は、
受信トレイに残したまま既読にする。ただし放置すると受信トレイが埋まるので、
一定期間で外す。

保持期間を**ルールの属性**にした。ラベルの属性ではない。

- 同じラベルでも送信元によって残しておきたい期間が違いうる
- `labels` は `importCurrentState()` が Gmail の実態を写す**スナップショット**で、
  人が育てる場所ではない。振り分けの意図は `rules` に置く (真実の源はシート)
- 空欄なら外さない。**既定は「何もしない」**で、設定した分だけ動く

外すのは受信トレイからだけで、削除はしない。Gmail の「すべてのメール」には残る。
`Security` に設定しないことを運用の約束にする — 不正利用の警告を流してはいけない。

### なぜ `unmatched` を受信トレイに限るか

アーカイブ済みが混ざると「受信トレイから外すべきか」の判断材料として濁ります。
Gmail のカテゴリ (`promotions` / `social` / `updates`) は既に分類された結果なので、
受信トレイ除外の根拠にそのまま使えます。

ただしカテゴリだけでは決めません。`updates` には請求書や認証コードが混ざるため、
未読率と合わせて初めて候補にします。どのタブにも入らない `(なし)` は候補にしません。

### 提案を誰が立てるか

当初は週次 AI (Claude の Routine) が `unmatched` を読んで `proposals` を書く想定だった。
しかし **Google Drive のコネクタはファイルの中身を書けない** (`update_file` は
ファイル名と置き場所しか変えられない)。Routine からシートへ追記する手段が無い。

そこで提案を 2 つに分けた。

| 提案 | 誰が立てるか | 理由 |
| --- | --- | --- |
| `inbox_skip` (受信トレイから外すか) | **GAS** | 判断が要らない。Gmail のカテゴリと未読率がそのまま答え |
| `new_rule` / `new_label` (どのラベルへ) | 人 (将来は AI) | ジャンルの判断は機械にはできない |

`inbox_skip` は `unmatched` の `除外候補` 列がそのまま提案になる。外部依存も、
他人が書いた内容を取り込む経路も要らない。**承認ループが実際に回り始める**。

ラベルの判断が要る提案は、往復の仕組みを作る前に**提案の質が使い物になるか**を
確かめてから決める。先に配管を作って流すものが期待外れ、という順序を避ける。

### 提案の形

ご要望の 3 つがそのまま乗ります。

- 「定期購読しているメールを○○のラベルに振り分けませんか？」 → `new_rule`
- 「項目を新たに作って、このメーリングリストを振り分けますか？」 → `new_label` + `new_rule`
- 「○○からの受信が増えていますが、受信トレイから外しますか？」 → `inbox_skip`

---

## 5. サービス選定

| レイヤ | 採用 | 理由 |
| --- | --- | --- |
| DB | Google スプレッドシート | ご指定通り。GAS からネイティブに読める |
| 実行基盤 | **Google Apps Script** | 後述 |
| ソース管理 | このリポジトリ + clasp + TypeScript | GAS の Web エディタは保守が辛い |
| 週次 AI | Claude の Routine | 決定済み |
| 通知 | 自分宛メール + シート | |

### 5.1 なぜ GAS か (対抗案との比較)

**対抗案: Python + GitHub Actions (cron) + Gmail API**

一見きれいですが、個人 Gmail では**OAuth の罠**があります。Google Cloud プロジェクトを作り
同意画面を設定する必要があり、公開ステータスが「テスト」のままだと
**リフレッシュトークンが 7 日で失効**します。毎週手で認証し直すことになり、
「週次で自動実行」という目的と正面から衝突します。回避するには本番公開への
切り替えが要り、個人利用にしては手間が大きい。

**GAS を採用する理由:**

- OAuth クライアント / サービスアカウント / シークレット管理が**一切不要**
- `GmailApp` と `SpreadsheetApp` が**同一認証**で使える
- 時間主導トリガーが**PC の電源に依存せず**動く
- 無料

### 5.2 GAS の制約と対処

| 制約 | 対処 |
| --- | --- |
| 実行 6 分 / 回 | 遡及適用は `PropertiesService` にカーソルを保存する「続きから」方式で分割実行 |
| `GmailApp` 日次クォータ (読み取り系 20,000 操作 / 日 程度) | 遡及バッチはクォータ残を監視し、閾値で自走停止 |
| `GmailApp.search()` の 1 回あたり件数 | 500 件を目安に `start` / `max` でページング |

### 5.3 Claude Routine の未検証点 (正直に記載)

週次 Routine のセッションから **Gmail / Drive の MCP が使えるかは実装時に検証が必要**です。
使えなかった場合のフォールバックを用意します。

- GAS が週次ダイジェストを**シートに書き、同時に自分宛メールでも送る**
- Routine はシート (Drive MCP) かメール (Gmail MCP) のどちらか使える方を読む
- どちらも不可なら、GAS + Gemini API に切り替え (この場合も `proposals` シートの
  スキーマは変わらないので、書き手だけ差し替えれば済む設計にしておく)

### 5.4 テストをどう回すか

#### 障害

GAS は**モジュール構文を受け付けない**。そのため `src/` は `import` / `export` を
一切持たず、全ファイルがフラットなグローバル名前空間で書かれている。
Node からそのまま読み込めない。

#### 選択肢

| 案 | 内容 | 却下した理由 |
| --- | --- | --- |
| A | `src/` に `export` を書き、push 前に剥がすビルドを挟む | `npm run push` が単純な `clasp push` でなくなる。**剥がし漏れが即障害になる** |
| B | 純粋ロジックを別ディレクトリへ切り出し、通常の TypeScript にする | clasp は `src/` 全体を push するので、結局 A と同じビルドが要る |
| C | テストからファイルを 1 本ずつ読んで評価する | 依存する定数の**読み込み順を手で管理する**ことになる。ファイルが増えるたびに壊れる |
| **D** | **`tsc` の `outFile` で 1 本へ連結し、その中の関数を取り出す** | **採用** |

#### 採用した形 (D)

```
tsconfig.test.json  ← 基本設定を継承し、noEmit を外して outFile へ出す
        ↓ tsc
1 本の JS            ← 読み込み順は tsc が解決する
        ↓ Node が読み、末尾で関数を返す
node --test で検査
```

この形を選んだ理由は 3 つ。

- **`src/` に手を入れない。** GAS 用の構成をテストの都合で歪めない
  (`CLAUDE.md` の「実装の都合でモデルを歪めない」と同じ理由)
- **`npm run push` は `clasp push` のまま。** ビルド工程が増えないので、
  push した内容とリポジトリの内容が常に一致する
- **読み込み順を手で管理しない。** ファイルを足しても壊れない

成立の前提として、**`src/` のトップレベルで GAS API を呼んでいない**ことを確認してある。
定数と型注釈しか無いので、Node で読み込んでも実行時に落ちない。
この前提が崩れる変更をするときは、ここを読み直す。

#### 何をテストし、何をしないか

| 対象 | 手段 |
| --- | --- |
| GAS API に触れない純粋関数 | **`node --test`** |
| `GmailApp` / `SpreadsheetApp` を呼ぶ層 | `npm run typecheck` と、実データに対するシート検査 |
| シートの状態 | メニュー「シートを検査する」と週次ダイジェスト |

GAS API を呼ぶ層に偽物を差し込む仕組みは**今は作らない**。
`matcher.ts` を GAS 非依存に保つ設計 (SOLID の項) が効いていて、
判断ロジックの多くは純粋関数側にある。偽物が必要になってから考える。

#### `runMatcherSelfTest()` は廃止した

GAS 上でしか動かず、人がメニューから実行しないと結果が分からなかった。
検証は `npm test` へ移し、関数とメニュー項目を削除した。**同じ検証を 2 箇所に置かない。**

---

---

## 6. 導入ステップ

| # | 内容 | 備考 |
| --- | --- | --- |
| 1 | スプレッドシート `Gmail Label DB` を作成 | 空でよい |
| 2 | コンテナバインド型の GAS プロジェクトを作成 | シートの拡張機能メニューから |
| 3 | clasp セットアップ、`src/` を push | このリポジトリ |
| 4 | `setup()` 実行 — 8 シート生成 + 入力規則 + 条件付き書式 + トリガー登録 | |
| 5 | **`importCurrentState()` 実行** — 既存 39 ラベルと Gmail フィルタをシートへ取り込む | **初期登録コストがここでゼロになる** |
| 6 | ラベル体系を新体系へ移行 | **完了済み。専用の仕組みは撤去した** |
| 7 | `DRY_RUN = true` で実行 → `log` を確認 | **誤爆の確認はここだけ** |
| 8 | 本適用 (新着のみ) を 15 分トリガーで開始 | |
| 9 | 遡及バッチ (直近 1 年) を分割実行 | 以降は月次トリガーが取りこぼしを回収 |
| 10 | 週次 Claude Routine を設定 | |
| 11 | ログで一致を確認できた既存 Gmail フィルタから順に削除 | 段階移行 |

ステップ 5 が効きます。「登録が面倒」という課題に対し、**今ある 39 ラベルと
既存フィルタの `from:` を機械的に吸い出して初期データにする**ので、
手入力はほぼ発生しません。

---

## 7. 決着した論点

設計時に「推測で決めず実測してから確定する」としていた点は、全て決着しました。

| 論点 | 結論 |
| --- | --- |
| `Australia/Stores` に取引メールが混ざっていないか | 混ざっていなかった。標本 50 スレッドは全て販促。`Promotions/Stores` + `@AU` で確定 |
| 求人の送信元内訳 | 3 ドメインでほぼ説明がつく。`kuraveil.jp` / `seek.com.au` → `Alerts`、`doda` 系 → `Agencies` |
| `Work/Applications` の遡及判定 | 「スレッドに自分の送信あり」が有効。応募はメールで直接やり取りしており、プラットフォーム内で完結していなかった |
| `Promotions` を受信トレイ除外の既定にしてよいか | よい。ただし**除外して既読にしてよいのはここだけ**という線引きを守る |
| 未読を既読にするか | `Promotions` 配下のみ。`Finance` / `Security` / `Work` は未読のまま残す |

## 参考

- [Manage Gmail filters — Google Workspace 開発者ドキュメント](https://developers.google.com/workspace/gmail/api/guides/filter_settings)
- [Gmail Limits Explained (2026)](https://www.mailjerry.com/gmail-limits/)
- [Google Apps and Gmail limits](https://hiverhq.com/blog/gmail-and-google-apps-limits-every-admin-should-know)
