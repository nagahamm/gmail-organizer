# gmail-organizer

Gmail のラベル振り分けを Google スプレッドシートで一元管理し、Google Apps Script (GAS) で
自動適用・ログ記録し、週次で AI が改善提案を出す仕組み。

## Core Principles

共通原則(SOLID/KISS/YAGNI/DRY)はグローバル `~/.claude/CLAUDE.md` の通り。
本プロジェクト固有の適用:

- **SOLID**: 照合 (`matcher.ts`) / 適用 (`applier.ts`) / シート入出力 (`sheets.ts`) /
  検査 (`validate.ts`) / 提案 (`proposer.ts`) の実装をまたいで混ぜない。
  `matcher.ts` は GAS API に依存しない純粋関数のまま保つ。
- **YAGNI**: ラベルも同じで、**観測できていないカテゴリは作らない**。
- **DRY**: 列定義は `schema.ts` だけに置く。ドキュメントも同様で、詳細は下表の各ファイルに一度だけ書く。

## ドキュメント

一覧とリンクは [`README.md`](README.md) にある。実装前の必読は
`docs/constraints.md` (Gmail / GAS / 設計上の制約)。

## 設計の要点 — どこに書いてあるか

理由や詳細は書かない。**探す先だけを示す。**

| 決定 | 詳しくは |
| --- | --- |
| 真実の源はスプレッドシート。Gmail フィルタは降格させて凍結し並走する | `docs/design.md` §2 |
| ラベルの軸は 2 本 (種別 / ジャンル / ブランド)、拠点は並行ラベル | `docs/naming.md` |
| 除外して既読にしてよいのは `Promotions` だけ | `docs/constraints.md` 設計上の制約 1 |
| 照合は `List-Id` を第一級に扱う。`from:` のワイルドカードは効かない | `docs/constraints.md` Gmail 側の制約 1 |
| 求人は `Promotions/Jobs` と `Work` に二分。境界は「自分がアクションしたか」 | `docs/design.md` §2.3 |
| 週次 AI 提案は承認列のプルダウンを変えるだけで `rules` に反映される | `docs/design.md` §3 |

## 構成

| レイヤ | 採用 |
| --- | --- |
| DB | Google スプレッドシート (`Gmail Label DB`) |
| 実行基盤 | Google Apps Script (コンテナバインド型) |
| ソース管理 | このリポジトリ + clasp + TypeScript |
| 週次 AI | Claude の Routine |

GAS を選んだ理由: OAuth クライアント / シークレット管理が**一切不要**で、
`GmailApp` と `SpreadsheetApp` が同一認証で使える。トリガーが PC の電源に依存しない。無料。

## 開発

```bash
npm install
npx clasp login          # 初回のみ
npm run typecheck        # 変更したら必ず通す
npm run push             # src/ を GAS プロジェクトへ反映
```

`.clasp.json` の `scriptId` は各自の GAS プロジェクト ID。**コミットしない** (`.gitignore` 済み)。

## 守ること

- **破壊的操作は必ずドライランを先に通す**。`DRY_RUN = true` の実行ログをシートで確認してから本適用する。
- **メール本文をログに書かない**。件名は先頭 60 文字まで。
- ラベルの**削除は提案のみ**。実削除は人間が Gmail 上で行う (誤削除の巻き戻しが効かないため)。
- ルールの追加・変更は**シート側が正**。コード内にドメインやアドレスをハードコードしない。
- **メールを送るときは必ず HTML (`htmlBody`) にし、人が読んで見やすい形にする**。
  「機械向けだから見た目は気にしない」は誤り。メールである以上、受信トレイに届いて
  人の目に触れる。GAS が読み取るための中継メール・開発用データ投入メールも例外ではない
  (`docs/design.md` 5.6)。
- **利用者がラベルの扱いを口頭で決めたら、その場で `docs/` に書き起こす**。次項。
- **使い切りの復旧処理をスプレッドシートのメニューに登録しない**。データ不整合の
  一度きりの修正など、繰り返し使わない関数は GAS API に触れる関数として書くだけに留め、
  Apps Script エディタの「関数を選択して実行」から直接呼ぶ。メニューは日常的・
  繰り返し使う操作だけの場所とし、使い切りの関数を足すたびに肥大化させない。

## 利用者が決めたラベルの扱いは docs に残す

「この送信元はこのラベル」「これは受信トレイから外す」といった判断を利用者から
受け取ったら、**Issue とシートだけで済ませない**。必ず `docs/` にも書く。

| 何を | どこへ |
| --- | --- |
| ラベルの新設・改名・行き先の変更 | `docs/naming.md` |
| 受信トレイ除外・既読化の線引き | `docs/constraints.md` 設計上の制約 |
| 利用者から見た振る舞いが変わるもの | `docs/requirements.md` のシナリオ |

**決定と一緒に理由を書く。** 「Containers for Change は `Promotions/Rewards`」だけでなく
「受信トレイから外れて見落とす側に倒れるが、金額が小さく期限も無いので許容する」まで残す。
理由が無いと、次に似た判断が来たときに必ずぶれる。

**使わなくなったら消す。** ラベルを廃止したら docs の記述も落とす。
作らないと決めた枠や、観測できなくなったカテゴリの表も残さない。
`docs/` は「現在どうなっているか」なので、**古い記述は嘘になる**。
実装より docs を信じた次の判断が壊れる。

歴史を残したいものは Issue に書く。`docs/` に「かつてこうだった」を積まない。

## 進め方

プロジェクトの規模が大きくなったため、**思いつきで実装を始めない**。次の順序を守る。

### 1. 要件定義 (BDD 寄り)

機能に関わる変更は、着手前に `docs/requirements.md` へ Gherkin 形式 (Given / When / Then) の
シナリオを追記・更新する。**ステークホルダー (利用者本人) が読んで齟齬に気づけること**を
優先し、実装用語ではなく振る舞いで書く。

### 2. 詳細設計 (DDD 寄り)

ドメインモデル・シート設計に関わる変更は、`docs/design.md` を先に更新する。
業務上の関心事 (ラベル / ルール / 送信元 / 提案) を中心に据え、
**実装の都合でモデルを歪めない**。

### 3. 実装 (TDD 寄り)

検証可能な範囲はテストを用意し、通してからコミットする。すべてを先にテストから書く
厳密な TDD までは求めないが、**テストなしで実装だけのコミットはしない**。

`npm test` が `docs/requirements.md` のシナリオを Node で検証する。
仕組みは `docs/design.md` 5.4 にある。GAS API に触れない純粋関数だけが対象で、
`GmailApp` / `SpreadsheetApp` を呼ぶ層は `npm run typecheck` とシート検査に任せる。

### AIDD

本プロジェクトの実装は Claude Code が**要件定義からコード・テストまで一貫して担う**。
上記の手順 (要件定義 → 設計 → 実装) を飛ばして直接コードから着手しない。

### Issue と docs の役割

Issue 起点の開発・質問可否・確認方針はグローバル `~/.claude/CLAUDE.md` の通り。

Issue は成果物 (要件定義のシナリオ・設計書の該当箇所) への言及を含めてよいが、
**要件定義書・設計書そのものの代わりにはならない**。
Issue は「何をなぜやるか」の記録、`docs/` は「現在どうなっているか」の生きたドキュメント。

## コミット

Git Commit Rules はグローバル `~/.claude/CLAUDE.md` の通り。テンプレートの Issue 参照は
本プロジェクトでは `Closes #<issue番号>` を既定とする (機能追加が主体のため)。

プロジェクト固有の追加: **各コミットは単体で成立させる**。どの時点でチェックアウトしても
`npm run typecheck` が通る状態を保つ。

## タグ

タグ運用はグローバル `~/.claude/CLAUDE.md` の通り。

プロジェクト固有の追加: **タグを打つコミットで `package.json` の `version` も同じ番号にする。**
版を名乗る場所が 2 つあるので、揃える手順を決めておかないと必ずずれる。
正はタグで、`package.json` はそれに追従する。
