# gmail-organizer

Gmail のラベル振り分けを Google スプレッドシートで一元管理し、Google Apps Script (GAS) で
自動適用・ログ記録し、週次で AI が改善提案を出す仕組み。

## なぜ作るか

Gmail 標準のフィルタで運用していたが、以下の理由で破綻していた。

1. フィルタは**受信時にしか走らない**。ルールを直しても過去メールに遡及しない。
2. `from: *.agoda-emails.com` のような**ワイルドカードを Gmail は解釈しない**。意図と違うマッチをする。
3. 1 アカウント **1,000 フィルタ / 1 フィルタ 1,500 文字**の上限。`from:({ a b c })` で束ねる書き方は
   上限回避の苦肉の策で、育つと壊れる。
4. **ログが残らない**。何がなぜ振り分けられた / されなかったかを追えない。
5. 「受信トレイをスキップ + 既読」が効くとメールが静かに消えるため、失敗に気づけない。

結果として受信トレイに 11,642 通 / 未読 9,225 通が滞留していた (2026-08-19 時点)。

## 設計の決定事項

詳細は `docs/design.md`。要点のみ:

- **ラベル名は英語**。大項目・中項目は英語、小項目のブランド名のみ原語 (`Rakuten` / `Amazon` / `AWX`)。
  命名規約は `docs/naming.md`。要点は「可算名詞は複数形、不可算名詞は単数形」
  (`Orders` / `Cards` に対して `Finance` / `Fashion`)、略語と動詞を使わない。
- **ラベルの軸**: 大項目 = メールの**種別** (`Finance` / `Orders` / `Promotions` /
  `Security` / `Schedule` / `Work` / `Support` / `Official` / `Subscriptions` /
  `Health` / `Personal`)、中項目 = ジャンル、小項目 = ブランド。
  **拠点** (`@AU`) は階層に混ぜず**並行ラベル**として付与する。Gmail はフォルダではなく
  タグなので二重付与が自然。`@JP` は作らない (`@AU` でないものは全部 JP のため)。
- **ワーホリは終了済みなので `@AU` は最初からアーカイブ**。移行時に既存メールへ
  遡及付与するだけで、新規メールには付けない (AU 関連ルールの `凍結日` を移行日にする)。
  ラベル自体は過去メールの検索性のために残す。
- **求人は `Promotions/Jobs` と `Work` に二分する**。境界線は「自分がアクションしたか」。
  読み流す側 (`Promotions/Jobs/Agencies` / `Temp` / `Alerts`) は受信トレイ除外 + 既読、
  自分が動いている側 (`Work/Applications` / `Shortlist` / `AWX`) は受信トレイに残す。
  `Work/Applications` は「スレッドに自分の送信が含まれるか」で判定し、提案として出す。
  `Work/Shortlist` は**スターを入力装置**にし、スター付与を検知してラベルへ昇格させる。
- **真実の源はスプレッドシート**。Gmail フィルタは真実の源から降格させる。
- **マッチングは `List-Id` を第一級に扱う**。メーリングリストは `List-Id` /
  `List-Unsubscribe` ヘッダを持つので Gmail 検索の `list:` 演算子で確実に引ける。
  `from:` のワイルドカードより桁違いに安定する。
- **既存フィルタは凍結して並走**し、ログで一致を確認できたものから順に Gmail 側を削除する。
- **遡及適用は直近 1 年**。ドライラン → ログ確認 → 本適用の順。
- **週次 AI 提案は Claude の Routine**。提案はシートに書き込み、承認列のプルダウンを変えるだけで
  次回実行がルールに反映する (「登録が面倒」の解消)。

## 構成

| レイヤ | 採用 |
| --- | --- |
| DB | Google スプレッドシート (`Gmail Label DB`) |
| 実行基盤 | Google Apps Script (コンテナバインド型) |
| ソース管理 | このリポジトリ + clasp + TypeScript |
| 週次 AI | Claude の Routine |

GAS を選んだ理由: OAuth クライアント / サービスアカウント / シークレット管理が**一切不要**で、
`GmailApp` と `SpreadsheetApp` が同一認証で使える。時間主導トリガーが PC の電源に依存せず動く。無料。

## 開発

```bash
npm install
npx clasp login          # 初回のみ
npx clasp push           # src/ を GAS プロジェクトへ反映
npx clasp open           # GAS エディタを開く
```

`.clasp.json` の `scriptId` は各自の GAS プロジェクト ID。**コミットしない** (`.gitignore` 済み)。

## GAS の制約と対処

- **実行 6 分 / 回**: 遡及適用は `PropertiesService` にカーソルを保存する「続きから」方式で分割実行。
- **`GmailApp` 日次クォータ**: 無料アカウントは読み取り系 20,000 操作 / 日 程度。
  遡及バッチはクォータ残を監視し、閾値で自走停止する。
- **`GmailApp.search()`**: 1 回あたり 500 件を目安に `start` / `max` でページング。

## 作業上の約束

- **破壊的操作は必ずドライランを先に通す**。`DRY_RUN = true` の実行ログをシートで確認してから本適用する。
- **メール本文をログに書かない**。件名は先頭 60 文字まで。送信元アドレスとラベル名以外の
  個人情報をシートに残さない。
- ラベルの**削除は提案のみ**。実削除は人間が Gmail 上で行う (誤削除の巻き戻しが効かないため)。
- ルールの追加・変更は**シート側が正**。コード内にドメインやアドレスをハードコードしない。

## 進め方

- **Issue を起点にする**。着手前に Issue を立て、症状・原因・対応方針を残してから実装する。
  バグ修正・機能追加・調査のいずれも対象。typo やフォーマット修正など、説明の必要がない
  些細な変更は除く。
- **作業中は確認しない**。修正・テスト・コミットまでを一括で実行し切る。
- **質問してよいのは 2 つの場合のみ**。着手前の前提確認と、予期せぬ破壊的変更のリスクがあり
  判断不能な場合。

## コミット

Conventional Commits に準拠する。**header は英語、本文は日本語**。

```
<type>(<scope>): <short summary in English>

- 変更点を日本語で箇条書き

Closes #<issue番号>
```

- 対応する Issue を `Fixes #N` (バグ修正) または `Closes #N` (機能追加) で必ず閉じる。
  1 つの Issue に複数コミットが紐づく場合は、最後のコミットにのみ書く。
- **1 コミット = 1 タスク**。「なぜこの変更をしたか」が 1 つの理由で説明しきれる単位に分割する。
- 性質の異なる変更 (バグ修正 / リファクタリング / 新機能 / ドキュメント) は、
  同じファイルを触る場合でも必ず分ける。
- **各コミットは単体で成立させる**。どの時点でチェックアウトしても `npm run typecheck` が通る。
- バグ修正が先、リファクタリングが後。
- 直前のコミットの修整・補足であれば `--amend` で統合する。別タスクなら必ず新規コミットにする。

## タグ

`main` へマージした単位で `vMAJOR.MINOR.PATCH` の注釈付きタグを打つ。
コミットの type ではなく**利用者から見た変化の大きさ**で決める。

- PATCH: バグ修正、内部改善、使い方が変わらないもの
- MINOR: 新しくできるようになったことがある
- MAJOR: 全面刷新、または旧設定のままでは動かない非互換変更

`docs` / `refactor` のみの変更ではタグを打たない。
利用者側の再設定が必要な場合は、タグの注釈に手順を明記する。
