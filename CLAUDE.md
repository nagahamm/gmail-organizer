# gmail-organizer

Gmail のラベル振り分けを Google スプレッドシートで一元管理し、Google Apps Script (GAS) で
自動適用・ログ記録し、週次で AI が改善提案を出す仕組み。

## Core Principles

- **SOLID**: 各モジュールの責務を保つ。照合 (`matcher.ts`) / 適用 (`applier.ts`) /
  シート入出力 (`sheets.ts`) / 検査 (`validate.ts`) の実装をまたいで混ぜない。
  `matcher.ts` は GAS API に依存しない純粋関数のまま保つ。
- **KISS**: 素直に書く。動く最小の実装を選ぶ。
- **YAGNI**: 今必要でない設定項目・抽象化・オプションを足さない。
  ラベルも同じで、**観測できていないカテゴリは作らない**。
- **DRY**: 同じロジック・同じ定数を二度書かない。既存の関数を再利用できるならそちらを使う。
  列定義は `schema.ts` だけに置く。ドキュメントも同様で、詳細は下表の各ファイルに一度だけ書く。

## ドキュメント

一覧とリンクは [`README.md`](README.md) にある。実装前の必読は
`docs/constraints.md` (Gmail / GAS / 設計上の制約)。

## 設計の要点 — どこに書いてあるか

理由や詳細は書かない。**探す先だけを示す。**

| 決定 | 詳しくは |
| --- | --- |
| 真実の源はスプレッドシート。Gmail フィルタは降格させて凍結し並走する | `docs/design.md` §2 |
| ラベルの軸は 2 本 (種別 / ジャンル / ブランド)、拠点は並行ラベル | `docs/naming.md` |
| 除外して既読にしてよいのは `Promotions` だけ | `docs/constraints.md` 設計上の制約 1 |
| 照合は `List-Id` を第一級に扱う。`from:` のワイルドカードは効かない | `docs/constraints.md` Gmail 側の制約 1 |
| 求人は `Promotions/Jobs` と `Work` に二分。境界は「自分がアクションしたか」 | `docs/design.md` §2.3 |
| 週次 AI 提案は承認列のプルダウンを変えるだけで `rules` に反映される | `docs/design.md` §3 |

## 構成

| レイヤ | 採用 |
| --- | --- |
| DB | Google スプレッドシート (`Gmail Label DB`) |
| 実行基盤 | Google Apps Script (コンテナバインド型) |
| ソース管理 | このリポジトリ + clasp + TypeScript |
| 週次 AI | Claude の Routine |

GAS を選んだ理由: OAuth クライアント / シークレット管理が**一切不要**で、
`GmailApp` と `SpreadsheetApp` が同一認証で使える。トリガーが PC の電源に依存しない。無料。

## 開発

```bash
npm install
npx clasp login          # 初回のみ
npm run typecheck        # 変更したら必ず通す
npm run push             # src/ を GAS プロジェクトへ反映
```

`.clasp.json` の `scriptId` は各自の GAS プロジェクト ID。**コミットしない** (`.gitignore` 済み)。

## 守ること

- **破壊的操作は必ずドライランを先に通す**。`DRY_RUN = true` の実行ログをシートで確認してから本適用する。
- **メール本文をログに書かない**。件名は先頭 60 文字まで。
- ラベルの**削除は提案のみ**。実削除は人間が Gmail 上で行う (誤削除の巻き戻しが効かないため)。
- ルールの追加・変更は**シート側が正**。コード内にドメインやアドレスをハードコードしない。

## 進め方

- **Issue を起点にする**。着手前に Issue を立て、症状・原因・対応方針を残してから実装する。
  typo やフォーマット修正など、説明の必要がない些細な変更は除く。
- **作業中は確認しない**。修正・テスト・コミットまでを一括で実行し切る。
- **質問してよいのは 2 つの場合のみ**。着手前の前提確認と、予期せぬ破壊的変更のリスクがあり
  判断不能な場合。

## コミット

Conventional Commits に準拠する。**header は英語、本文は日本語**。

```
<type>(<scope>): <short summary in English>

- 変更点を日本語で箇条書き

Closes #<issue番号>
```

- 対応する Issue を `Fixes #N` (バグ修正) または `Closes #N` (機能追加) で必ず閉じる。
  1 つの Issue に複数コミットが紐づく場合は、最後のコミットにのみ書く。
- **1 コミット = 1 タスク**。性質の異なる変更は同じファイルを触る場合でも必ず分ける。
- **各コミットは単体で成立させる**。どの時点でチェックアウトしても `npm run typecheck` が通る。
- バグ修正が先、リファクタリングが後。
- 直前のコミットの修整・補足であれば `--amend` で統合する。別タスクなら必ず新規コミットにする。

## タグ

`main` へマージした単位で `vMAJOR.MINOR.PATCH` の注釈付きタグを打つ。
コミットの type ではなく**利用者から見た変化の大きさ**で決める。

- PATCH: バグ修正、内部改善、使い方が変わらないもの
- MINOR: 新しくできるようになったことがある
- MAJOR: 全面刷新、または旧設定のままでは動かない非互換変更

`docs` / `refactor` のみの変更ではタグを打たない。
